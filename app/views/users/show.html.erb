<section class="dashboard-main">
  <h1> <%= current_user.first_name %>'s Dashboard </h1>

  <%= button_to 'Log Out', logout_path, method: 'delete', class: "btn btn-primary mb1 bg-teal" %>
  <h3>Account Details</h3>
  <ul>
    <li> <%= current_user.first_name  %> <%= current_user.last_name %> </li>
    <li> <%= current_user.email%> </li>
  </ul>
  <section>
    <h1>Bookmarked Segments</h1>
  </section>
<<<<<<< HEAD
  <%= link_to "Connect to Github", "/auth/github", class: "btn btn-primary mb1 bg-teal" %>
  <% if !current_user.token.nil? %>
    <section>
      <h1>Github</h1>
      <% facade.github_repos.each do |repo| %>
      <ul class="repo">
          <li class="repo_name"> <%= link_to repo.name, repo.html_url %></li>
        </ul>
        <% end %>
      </section>
      <section>
        <h1>Followers</h1>
        <% facade.github_followers.each do |follower| %>
        <%= link_to follower.login, follower.html_url %>
        <% end %>
=======
  <section>
    <h1>Github</h1>
    <% facade.github_repos.each do |repo| %>
    <ul class="repo">
        <li class="repo_name"> <%= link_to repo.name, repo.html_url %></li>
      </ul>
    <% end %>
  </section>
  <section class ="followers">
    <h1>Followers</h1>
    <% facade.github_followers.each do |follower| %>
    <ul>
      <li class ="follower_name"><%= link_to follower.login, follower.html_url %></li>
    </ul>
    <% end %>
    </section>
    <section class="following">
      <h1>Following</h1>
      <% facade.github_following.each do |following| %>
      <ul>
        <li class ="following_name"><%= link_to following.login, following.html_url %></li>
      </ul>
      <% end %>
>>>>>>> 0c7be3e69311c689658a33ab811ab5c374e46c84
      </section>
      <section class="following">
        <h1>Following</h1>
        <% facade.github_following.each do |following| %>
          <%= link_to following.login, following.html_url %>
          <% end %>
        </section>
  <% end %>
</section>
